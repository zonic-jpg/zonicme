# Not the publish root (netlify.toml publishes "."). Kept in sync with /_redirects
# so nobody edits this copy and expects it to take effect.
/admin             /admin.html   200
/config            /admin.html   302
/hub               /             301
/zonicme-hub.html  /             301
